/**
 * The editor's toolbar icons, from this repo's own Phosphor set
 * (`DESIGN_SYSTEM.md › Rules`) rather than the reference kit's inline SVGs —
 * one icon library across `/admin`, not a second one bundled with the
 * editor.
 */
export {
  ArrowCounterClockwise as IconUndo,
  ArrowClockwise as IconRedo,
  BezierCurve as IconSpline,
  CaretLeft as IconPanelClose,
  CaretRight as IconPanelOpen,
  Cursor as IconPointer,
  Magnet as IconMagnet,
  Polygon as IconPencil,
  Square as IconSquare,
  Trash as IconTrash,
} from '@phosphor-icons/react/dist/ssr';
